apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-ingress.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "helm-ingress.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
